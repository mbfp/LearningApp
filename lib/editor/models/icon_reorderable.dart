/// use this interface for editor tiles, when the widget can't get reordered in 
/// the sliverReorderableWidget with a long hold, but rather with an icon to hold
abstract class IconReorderable{
  bool showReorderIcon = false;
}
